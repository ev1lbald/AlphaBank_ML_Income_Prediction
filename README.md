# AlphaBank Income Prediction Service

Сервис для прогнозирования дохода клиентов Альфа-Банка с использованием Machine Learning. Включает в себя пользовательский интерфейс для сотрудников фронт-офиса и REST API для интеграции с ML-моделями.

## 🏗 Структура проекта

- **backend/** — FastAPI приложение (API, работа с БД, заглушка ML-модели).
- **frontend/** — React приложение (интерфейс сотрудника, витрина данных).
- **prototype/** — Исходные HTML/CSS прототипы.
- **docker-compose.yml** — Оркестрация контейнеров (Backend + PostgreSQL).

## 🚀 Быстрый старт

### 1. Запуск Backend и Базы данных
Убедитесь, что Docker запущен, затем выполните в корне проекта:

```bash
docker-compose up --build
```

- API документация (Swagger): http://localhost:8000/docs
- API endpoint: http://localhost:8000/api

### 2. Запуск Frontend
Откройте **новое окно терминала**, перейдите в папку frontend и запустите сервер разработки:

```bash
cd frontend
npm install  # Если запускаете первый раз
npm run dev
```

Интерфейс будет доступен по адресу: http://localhost:5173

## 📊 Импорт данных

Для загрузки клиентских данных в базу данных:

### Локально:
```bash
cd backend
python import_data.py
```

### На сервере (в Docker):
```bash
# Убедитесь, что файл backend/data/users.csv.zip существует
# Затем выполните импорт внутри контейнера:
sudo docker-compose -f docker-compose.prod.yml exec backend python import_data.py
```

## 🛠 Функционал

### Backend
- **Поиск клиентов**: Поиск по ID клиента.
- **Прогноз (ML Stub)**: Использует значение `target` из базы данных для прогноза дохода.
- **PostgreSQL**: Хранение профилей клиентов и истории прогнозов.

### Frontend
- **Дашборд клиента**: Отображение текущего и прогнозируемого дохода.
- **SHAP Объяснения**: Визуализация факторов, влияющих на решение модели.
- **Рекомендации**: Персональные предложения продуктов (NBA) и финансовые советы.
- **Метрики**: Мониторинг качества работы модели (MAE, R2, охват).

## 📦 Технологический стек

- **Backend**: Python 3.10, FastAPI, SQLAlchemy, Pydantic.
- **Database**: PostgreSQL 15.
- **Frontend**: React 18, Vite 5.
- **Infrastructure**: Docker, Docker Compose.

